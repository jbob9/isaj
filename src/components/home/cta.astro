---
import { ArrowRight, MessageSquare, Send, UserPlus } from "lucide-react";
---

<section class="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
  <div
    class="relative overflow-hidden rounded-[3rem] bg-slate-900 p-8 text-center shadow-2xl shadow-blue-900/20 md:rounded-[5rem] md:p-20"
  >
    {/* Floating Decorative Elements */}
    <div
      class="absolute top-12 left-12 hidden animate-bounce text-blue-500/20 md:block"
      style={{ animationDuration: "4s" }}
    >
      <Send size={48} className="rotate-[-20deg]" />
    </div>
    <div
      class="absolute right-12 bottom-12 hidden animate-pulse text-blue-400/10 md:block"
    >
      <svg width="120" height="120" viewBox="0 0 100 100" fill="currentColor">
        <circle
          cx="50"
          cy="50"
          r="40"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-dasharray="10 10"></circle>
      </svg>
    </div>

    <div class="relative z-10 mx-auto max-w-3xl">
      <span
        class="mb-8 inline-block rounded-full border border-blue-500/20 bg-blue-600/20 px-6 py-2 text-xs font-bold tracking-[0.2em] text-blue-400 uppercase"
      >
        Prêt à commencer ?
      </span>

      <h2 class="mb-8 text-4xl leading-tight font-bold text-white md:text-6xl">
        Développez pleinement <span class="font-serif text-blue-500 italic"
          >le potentiel</span
        > de votre enfant dès aujourd'hui.
      </h2>

      <p class="mb-12 text-lg leading-relaxed text-slate-400">
        Rejoignez les centaines de familles qui façonnent déjà l'avenir. Nous accueillons avec plaisir les nouveaux élèves et les enseignants passionnés.
      </p>

      <div class="flex flex-col items-center justify-center gap-6 sm:flex-row">
        <a href="/enrollment"
          class="group flex w-full transform items-center justify-center gap-3 rounded-4xl bg-blue-600 px-10 py-5 font-bold text-white shadow-xl shadow-blue-500/20 transition-all hover:scale-105 hover:bg-blue-700 sm:w-auto"
        >
          <UserPlus size={20} />
          Demande d'inscription
          <ArrowRight
            size={20}
            className="transition-transform group-hover:translate-x-1"
          />
      </a>

        <a
            href="/contact"
          class="flex w-full items-center justify-center gap-3 rounded-4xl border border-white/10 bg-white/5 px-10 py-5 font-bold text-white backdrop-blur-sm transition-all hover:bg-white/10 sm:w-auto"
        >
          <MessageSquare size={20} />
          Contactez le service des admissions
    </a>
      </div>

      <div
        class="mt-16 flex flex-col items-center justify-center gap-8 border-t border-white/5 pt-12 md:flex-row"
      >
        <div class="text-sm font-medium text-slate-500">
          Intéressé(e) à rejoindre notre équipe ?
        </div>
        <a
          href="/contact"
          class="group flex items-center gap-2 font-bold text-blue-500 transition-colors hover:text-blue-400"
        >
          Soumettez votre candidature <ArrowRight
            size={16}
            className="transition-transform group-hover:translate-x-1"
          />
      </a>
      </div>
    </div>

    {/* Dynamic Background Blurs */}
    <div
      class="absolute -top-24 -right-24 h-96 w-96 rounded-full bg-blue-600/10 blur-[100px]"
    >
    </div>
    <div
      class="absolute -bottom-24 -left-24 h-96 w-96 rounded-full bg-indigo-600/10 blur-[100px]"
    >
    </div>
  </div>
</section>
