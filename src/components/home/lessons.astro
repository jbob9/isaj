---
const lessons = [
  {
    title: "Letter Identification",
    level: "Class - Pre School",
    image:
      "https://images.unsplash.com/photo-1543269664-76bc3997d9ea?auto=format&fit=crop&q=80&w=200",
    bgColor: "bg-blue-100",
  },
  {
    title: "General Knowledge",
    level: "Fourth Grade",
    image:
      "https://images.unsplash.com/photo-1503454537195-1dcabb73ffb9?auto=format&fit=crop&q=80&w=200",
    bgColor: "bg-orange-100",
  },
  {
    title: "Geography Quiz",
    level: "First Grade",
    image:
      "https://images.unsplash.com/photo-1510531704581-5b2870972060?auto=format&fit=crop&q=80&w=200",
    bgColor: "bg-green-100",
  },
  {
    title: "Visual Arts Training",
    level: "Sketching class",
    image:
      "https://images.unsplash.com/photo-1508833313190-75077b77c5c2?auto=format&fit=crop&q=80&w=200",
    bgColor: "bg-yellow-100",
  },
];
---

<section class="bg-white py-20">
  <div class="mx-auto max-w-7xl px-4 text-center sm:px-6 lg:px-8">
    <p class="mb-4 text-sm font-medium text-gray-400">
      We focus on one impactful lesson at a time
    </p>
    <h2 class="mb-16 text-4xl font-bold text-[#3D2C26] md:text-5xl">
      Shaping the <span class="font-serif text-green-500 italic">future</span> of
      kids
    </h2>

    <div class="grid grid-cols-2 gap-8 lg:grid-cols-4">
      {
        lessons.map((lesson, idx) => (
          <div class="group cursor-pointer">
            <div
              class={`relative mx-auto h-32 w-32 rounded-full md:h-40 md:w-40 ${lesson.bgColor} mb-6 p-2 transition-transform duration-500 group-hover:rotate-12`}
            >
              <div class="h-full w-full overflow-hidden rounded-full border-4 border-white shadow-lg">
                <img
                  src={lesson.image}
                  alt={lesson.title}
                  class="h-full w-full object-cover"
                />
              </div>
              {/* Decorative blob behind */}
              <div class="absolute top-0 left-0 -z-10 h-full w-full scale-125 rounded-full bg-inherit opacity-20 blur-sm transition-all duration-700 group-hover:scale-150" />
            </div>
            <h3 class="mb-1 text-xl font-bold text-[#3D2C26] transition-colors group-hover:text-orange-500">
              {lesson.title}
            </h3>
            <p class="text-sm text-gray-500">{lesson.level}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
